<h1><%= @name %></h1>

<ol>
<% @tasks.each do |task| %>
<li><%= link_to task.name, task %>

<!-- 
	<%= form_tag("/#{task}/update", :method => 'PUT') do %>
<%= hidden_field :status, :value=>"Complete"%>	
	<%= submit_tag "Complete" %>
	<% end %>

 <%= form_for @task do |f| %>
     <%= f.hidden_field :status, :value=>"Complete"%>
     <%= f.submit "Complete" %>
   <% end %>

   <%= form_for @task do |f| %>
     <%= f.hidden_field :name, :value=>"Incomplete"%>
     <%= f.submit "Incomplete" %>
   <% end %> -->

 <br>
 <%= task.status %>
 <%= task.id %>
<!--   image_tag task.avatar.url(:thumb)
  form_for @task do |f|
  f.radio_button("task", "status", "Complete")Complete
 f.radio_button("task", "status", "Incomplete")Incomplete
 f.submit "Update"
end
 -->

<%= form_for @task, :url => '/tasks/:id', :method => 'put', 
  :html => { :multipart => true } do |form| %>
  <%= form.file_field :avatar %>
  <%= form.hidden_field :task_id, :value => task.id  %>
  <%= submit_tag "Add File" %>
<% end %>

</li>
<% end %>
</ol>


<%= form_for(@task) do |f|%>
<%= f.label :name %>
<%= f.text_field :name %>
<%= f.label :description %>
<%= f.text_area :description %>
<%= f.label :deadline %>
<%= f.date_field :deadline %>
<%= f.label :avatar %>
<%= f.file_field :avatar %>
<%= f.hidden_field :component_id, :value => @component.id %>

<%= f.submit "Create Task" %>
<% end %>

<h3>Users in Component</h3>
<% @component.users.each do |user|%>
<%= user.email %>
<% end %>

<h3> Add User to Component </h3>

<% @available_users = @available_users - @component.users %>

<ol> <% @available_users.each do |user|%>
<li> <%= user.email %> 
      <%= form_for @component, :url => "/components/:id", :method => "PUT" do |f| %>
      <%= f.hidden_field :component_id, :value => @component.id %>
      <%= f.hidden_field :user_id, :value => user.id %>
      <%= f.submit "Add" %>
      <% end %>
</li>
<% end %>

</ol>
<%= link_to "Home", root_path  %>